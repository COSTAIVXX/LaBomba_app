{
  "name": "labomba-functions",
  "version": "0.1.0",
  "private": true,
  "engines": {"node": "18"},
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext .ts src || true",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.4.0"
  },
  "devDependencies": {
    "typescript": "^5.2.0"
  }
}